(function f(
  vars0 = {
    x: undefined
  },
  body = (
    ctrl = 0,
    (
      vars0.x = 0
    ),
    vars0.x == 0 ? (
      (
        (function f(
          object = console,
          args = ["A"],
          body = object.log.customfunc ? object.log(args) : retval = object.log(...args)
        ) {})(),
        retval
      )
    ) : vars0.x == 1 ? (
      (
        (function f(
          object = console,
          args = ["B"],
          body = object.log.customfunc ? object.log(args) : retval = object.log(...args)
        ) {})(),
        retval
      )
    ) : vars0.x == 2 ? (
      (
        (function f(
          object = console,
          args = ["C"],
          body = object.log.customfunc ? object.log(args) : retval = object.log(...args)
        ) {})(),
        retval
      )
    ) : (
      (
        (function f(
          object = console,
          args = ["D"],
          body = object.log.customfunc ? object.log(args) : retval = object.log(...args)
        ) {})(),
        retval
      )
    )
  )
) {})();
